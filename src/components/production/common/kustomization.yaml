apiVersion: kustomize.config.k8s.io/v1alpha1
kind: Component
namespace: bank-of-anthos-production
commonLabels:
  environment: production
patches:
  - target:
      kind: Deployment
    patch: |-
      - op: replace
        path: /spec/template/spec/serviceAccountName
        value: gke-workload-production
